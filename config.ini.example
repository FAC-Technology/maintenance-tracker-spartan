[EQUIPMENT]
NAME = Equipment Name

[USERS]
# Comma-separated list of names for the dropdown
NAMES = Alice,Bob,Charlie

[SHEETS]
# Range where task data lives (first row of range = headers)
TASKS_RANGE = E2:L

[GOOGLE]
SHEETS_ID = <spreadsheet-id-from-url>
CREDENTIALS_FILE = credentials.json
